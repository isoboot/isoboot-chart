# Required: Network interface to bind to (e.g., enp4s0)
# This value MUST be provided during install
# interface: enp4s0

image:
  repository: alpine
  tag: latest
  pullPolicy: Always

resources:
  limits:
    memory: "128Mi"
    cpu: "500m"
  requests:
    memory: "64Mi"
    cpu: "100m"
