apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: responsetemplates.isoboot.io
spec:
  group: isoboot.io
  names:
    kind: ResponseTemplate
    listKind: ResponseTemplateList
    plural: responsetemplates
    singular: responsetemplate
    shortNames:
      - rt
  scope: Namespaced
  versions:
    - name: v1alpha1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          required:
            - spec
          properties:
            spec:
              type: object
              required:
                - files
              properties:
                files:
                  type: object
                  description: Map of filename to template content with Go template placeholders
                  minProperties: 1
                  additionalProperties:
                    type: string
                  x-kubernetes-validations:
                    - rule: "self.all(k, k.matches('^[A-Za-z0-9][-A-Za-z0-9_.]*$'))"
                      message: "File names must be valid path components (no slashes or path traversal)"
      additionalPrinterColumns:
        - name: Age
          type: date
          jsonPath: .metadata.creationTimestamp
